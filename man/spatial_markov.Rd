% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial-markov.R
\name{spatial_markov}
\alias{spatial_markov}
\title{Estimate Spatial Markov Transition Dynamics}
\usage{
spatial_markov(
  data,
  id,
  time,
  value,
  nb = NULL,
  listw = NULL,
  k = 5,
  lag_k = k,
  class_method = c("pooled_quantile", "time_quantile", "fixed"),
  zero.policy = TRUE
)
}
\arguments{
\item{data}{A long data frame or \code{sf} object.}

\item{id, time, value}{Columns identifying spatial unit, time, and value.}

\item{nb}{A \code{spdep} neighbor list, used only when \code{listw} is \code{NULL}. It is
converted with \code{spdep::nb2listw(style = "W")}.}

\item{listw}{A row-standardized \code{spdep} weights (\code{listw}) object. Preferred
when the standardization choice matters.}

\item{k}{Number of value classes.}

\item{lag_k}{Number of spatial-lag classes.}

\item{class_method}{Value classification method passed to \code{\link[=classify_dynamics]{classify_dynamics()}}.}

\item{zero.policy}{Passed to \code{spdep} lag/weight helpers.}
}
\value{
A \code{grd_spatial_markov} object.
}
\description{
Estimates transition matrices conditioned on the class of each unit's spatial
lag at the start of the transition period.
}
\examples{
\dontshow{if (identical(Sys.getenv("IN_PKGDOWN"), "true")) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
panel <- data.frame(
  id = rep(1:4, each = 3),
  year = rep(2020:2022, times = 4),
  value = c(1, 2, 3, 2, 3, 4, 4, 3, 5, 5, 6, 7)
)

listw <- spdep::nb2listw(spdep::cell2nb(2, 2), style = "W")
spatial <- spatial_markov(panel, id, year, value, listw = listw, k = 2)

spatial
lag_intervals(spatial)
transition_matrix(spatial, "count", lag_class = "Q1")
\dontshow{\}) # examplesIf}
}
