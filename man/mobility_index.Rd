% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ergodic.R
\name{mobility_index}
\alias{mobility_index}
\title{Markov Mobility Indices}
\usage{
mobility_index(
  x,
  measure = c("prais", "determinant", "eigen", "bartholomew1", "bartholomew2"),
  initial = NULL
)
}
\arguments{
\item{x}{A \code{grd_markov} object or a square transition probability matrix.}

\item{measure}{Character vector of indices to compute. Any of:
\itemize{
\item \code{"prais"}: the Prais--Shorrocks trace index, \code{(k - trace(P)) / (k - 1)}.
\item \code{"determinant"}: \verb{1 - |det(P)|}.
\item \code{"eigen"}: the Sommers--Conlisk index, one minus the modulus of the
second-largest eigenvalue of \code{P}.
\item \code{"bartholomew1"}: \code{(k - k * sum(initial * diag(P))) / (k - 1)}.
\item \code{"bartholomew2"}: the expected number of class boundaries crossed in
one transition, \verb{sum(initial[i] * P[i, j] * |i - j|) / (k - 1)}. Class
distance uses the class ordering, so classes are assumed ordinal.
}}

\item{initial}{Optional initial distribution over classes used by the
Bartholomew indices. Defaults to the uniform distribution, matching
\code{giddy}.}
}
\value{
A named numeric vector with one element per requested measure.
All indices are 0 for the identity matrix (no mobility) and increase
with mobility.
}
\description{
Computes scalar mobility indices summarising how much movement a transition
probability matrix implies, following Formby, Smith, and Zheng (2004) and
mirroring \code{giddy.mobility.markov_mobility()} in PySAL \code{giddy}.
}
\examples{
\dontshow{if (identical(Sys.getenv("IN_PKGDOWN"), "true")) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
prob <- matrix(c(0.9, 0.1, 0.2, 0.8), nrow = 2, byrow = TRUE)
mobility_index(prob)
mobility_index(prob, measure = "prais")
\dontshow{\}) # examplesIf}
}
