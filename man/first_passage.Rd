% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ergodic.R
\name{first_passage}
\alias{first_passage}
\title{Mean First-Passage Times}
\usage{
first_passage(x)
}
\arguments{
\item{x}{A \code{grd_markov} object or a square transition probability matrix.
The underlying chain must be ergodic (irreducible and aperiodic);
matrices with absorbing or unreachable classes are rejected.}
}
\value{
A square numeric matrix of mean first-passage times.
}
\description{
Computes the matrix of mean first-passage times for an ergodic transition
probability matrix following Kemeny and Snell: entry \verb{[i, j]} is the
expected number of transitions to reach class \code{j} for the first time
starting from class \code{i}. Diagonal entries are mean recurrence times,
the reciprocal of the stationary distribution.
}
\examples{
\dontshow{if (identical(Sys.getenv("IN_PKGDOWN"), "true")) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
prob <- matrix(c(0.9, 0.1, 0.2, 0.8), nrow = 2, byrow = TRUE)
first_passage(prob)
\dontshow{\}) # examplesIf}
}
